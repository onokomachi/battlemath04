# BattleMath:04 — code:CAT-WARS 設計ノート

kurarowa1（にゃんこ大戦争風・拠点建設＋襲撃バトル）を battlemath04（小4算数の学習ゲーム）に
融合・統合・最適化した際の設計判断を記録する。問題・解答方法は battlemath04 のものを踏襲し、
そこで稼いだ報酬で拠点を築き、ネコ軍団で相手拠点を襲撃する。

## 1. 残したもの / 削ったもの

| 区分 | 内容 |
|---|---|
| 残す | 練習・スピード・復習・本番テスト・きろく、バッジ／背景／称号／学校対抗／管理画面（すべて無変更） |
| 削る | カードバトル導線（`deck_building`/`in_game`）とカードパックショップ（`card_shop`）をメニューから撤去 |
| 追加 | `catwars` モード（拠点建設＋ネコ軍団の襲撃バトル） |

カードバトルの実装コードはリポジトリに残置し、メニュー導線とルーティングだけを断った（低リスク）。

## 2. ポイント／通貨設計（best practice・2通貨に集約）

**なぜ1通貨ではダメか**: 1通貨だと「大きな建設のために貯める（永続）」と「毎日その日に使い切る
（日替わり）」を**同時に両立できない**。逆に3通貨以上は子ども向けには複雑すぎる。そこで
**役割の異なる最小構成＝2通貨**とし、部隊XPは通貨ではなく自動成長として切り離した。

| 通貨 | 役割 | 入手 | 用途 | 性質 |
|---|---|---|---|---|
| 💠 クレジット (CR) | 永続通貨 | 問題に正解（難易度で傾斜。標準12・§12）／戦闘勝利の戦利品／金山のパッシブ収入 | レアネコの永続解放（傭兵召集） | 貯まる・長期進行 |
| ⚡ エナジー (EN) | 当日リセット通貨 | 今日の正解（難易度で傾斜。標準2・§12）／ログインボーナス | **陣地の施設を建設（§16）**、出撃バフ、戦闘の出撃コスト、戦闘中クイズで補充 | 今日ぶんは今日つかう＝毎日プレイの動機 |
| 部隊XP（通貨ではない） | ネコ個別の自動成長 | 学習で薄く（各スターター+4）／戦闘で厚く（勝35・負15） | レベル→進化（stage 1/2/3・姿とステータス変化） | 「学ぶ＝ネコが強くなる」教育的フック |

内部の store フィールド名は後方互換のため据え置き（`resources.gold`=CR / `dailyStars`=EN）。
「スター」等の幼稚な語感は廃し、`code:CAT-WARS` の世界観に合う SF 系の名称にした。

### 既存 MP（mathPoints）との関係
既存の MP システムは**温存**（バッジ／背景／称号／既存アイテムショップの原資）。CAT-WARS の
2通貨は MP と独立に、正解報酬イベントへ**加算的にフック**（`catwars/bridge.ts`）。後戻り可能・低リスク。

### 実装の要点（単一シンク）
全モード共通の解答記録 `services/weaknessAnalysisService.recordAttempt()` に
`grantStudyReward()` を1か所だけフック。練習・スピード・復習のすべての正解が自動的に
CR/EN/XP に変換される（重複計上なし）。本番テストは将来 `grantBulkReward` で完了時付与予定。

## 2.5 戦闘中クイズ（学習と戦闘の直結）

出撃前に**出題範囲を選択**（`RangeSelect`、スピードモードと同じ発想）。戦闘中の「📚といて⚡」から
その範囲の問題が**ランダム出題**され（`InBattleQuiz`）、正解すると ⚡エナジー を獲得してネコを
どんどん出撃できる（3連続でボーナス）。獲得量は問題の難易度・所要工程に応じて変わる（§12）。battlemath04 の問題データ・キーパッド・採点
（`checkAnswer`）をそのまま再利用。戦闘フュエル＝EN は「今日の学習」から生まれるため、
**算数を解くほど戦況が有利**になり、学習と戦闘が一体化する。戦闘中に扱う問題は text／選択肢／
guided に限定（複雑な図形・証明タイプは除外）してテンポを担保。

## 3. 拠点・管理構造

- **陣地づくり（BaseBuilder）**: ステージごとの自陣ゾーンに、⚡エナジーで施設を建てて配置する。
  施設は**当日リセット制**（もちもの・配置とも翌日リセット）。詳細は §16。
  施設は戦闘の能力（loadout）を解放：兵舎→兵種、大砲／テスラ→防衛、キャンプ→呪文の所持数、金山→パッシブ収入。
- **ステージ選択（StageSelectScreen）**: 章（＝マップ）を選ぶ。自陣ゾーンの広さは章ごとに違うため、
  ここで章を決めてから陣地を組む（詳細は §16）。
- **襲撃バトル（BattleScene）**: リアルタイム。⚡でネコを何度でも出撃、経路探索・ターゲッティング・
  呪文・敵ウェーブ・一時ランクアップ。敵コア破壊で勝利、戦利品 💠 と部隊XP を獲得。
- **管理画面**: 既存の GameMaster／単元ロック／学習分析／弱点分析を維持。CAT-WARS 固有の管理
  （敵拠点プリセット・報酬レート）は将来 `config/` に集約し Firebase で1リード同期する設計余地を残す。

## 4. バックエンド（後日 Firebase）

現状は**オフラインファースト**（zustand persist / localStorage）。kurarowa1 の Supabase 同期は
`catwars/utils/syncToSupabase.ts` を no-op スタブに置換して除去済み。Firebase 実装時は、この
スタブを差し替えるだけで各ストア（player/progress/army）のクラウド同期を有効化できる。
無料枠に配慮し「起動時1リード・区切りで1ライト（merge）」の粒度を維持する方針。PvP（人間対戦）も
このタイミングで追加予定（現状は当面 CPU 戦・オフライン）。

## 5. UI／デザイン

- タイトルを **BattleMath:04**＋サブタイトル **code:CAT-WARS**（青⇄白⇄赤グラデーション）に刷新。
- 全画面の基調を、青と赤が絶えず入れかわる**動的グラデーション背景**（`components/CyberGradientBG.tsx`、
  CSSアニメーションのみ・`prefers-reduced-motion` 対応）に置換。
- CAT-WARS ハブは 💠⚡🔥 の常時HUD、デイリー目標バー、大きな行動ボタン、拠点ミニプレビュー。
- ステージ選択：章カード＋敵紹介＋拠点プレビュー（ハブと同じ視覚言語。§8）。対戦画面：**青⇄赤の回転オーラ**を戦場背景に追加、
  ネオン枠・スプライト・投射体・ヒットエフェクト・ダメージ演出でリッチ化。

## 6. さらなるデザイン向上の提案（無課金で使える外部ツール／MCP）

このゲームは自己完結の Vite アプリなので、外部サービスは主に**ビルド時のアセット生成／取り込み**で
効果を発揮する（実行時は CSP 等の制約なくローカルアセットを同梱すればよい）。無課金で有効な選択肢：

1. **CC0 アセットの導入（最短・確実）** — Kenney.nl（40,000点超・CC0・登録不要）や OpenGameArt の
   CC0 パックを取り込み、UI・パーティクル・効果音・タイルを差し替えるだけで一段リッチになる。
   商用可・帰属表示不要でライセンスも安全。
2. **スプライト生成 MCP／API（無料APIキーで運用）** — 当初 Google AI Studio(Gemini)の画像生成
   （Nano Banana / Imagen）を検証したが、**無料枠は画像生成のリクエスト上限が0**（テキスト生成のみ無料、
   画像はGoogle Cloudのビリング有効化が必要）と判明したため不採用。代わりに
   **Pollinations.ai**（完全無料・APIキー不要の画像生成エンドポイント）を採用した。

## 7. ネコキャラクター33体の実制作（実施済み）

上記2の方針に基づき、**Pollinations.ai（無料・キー不要）＋自作の背景除去パイプライン**で
11系統×3進化段階＝**33体のキャラクタースプライトを実際に生成・組み込み済み**。

- **統一プロンプト設計**: 系統ごとに「固定アイデンティティ（毛色・系統の特徴）」＋「進化段階ごとの
  装備エスカレーション」を定義し（`スクリプト内 roster 相当の設定」）、共通スタイル文
  （bold outline・チビキャラ・にゃんこ大戦争を参照するが独自デザイン、と明記）を全生成で共有して
  画風のブレを抑えた。
- **背景透過処理**: Node.js + sharp でフラッドフィル型の背景除去（四隅の色を背景と推定→輪郭まで
  透過）→ バウンディングボックスでトリム→ 256×256 に正規化、を自作パイプライン化。
- **差し替え**: `catwars/data/characters.ts` の `getCharacterSprite()` の拡張子を `.svg`→`.png` に変更。
  旧SVG（kurarowa1由来の簡易ベクター画）は削除。参照点はこの1関数のみのため差し替えは局所的。
- **確認**: 開発サーバー起動→ヘッドレスChromiumで実画面を操作し、メインメニュー／CAT-WARSハブ／
  ネコ図鑑／出題範囲選択／ステージ選択／襲撃バトルの全画面でスクリーンショット確認ずみ。

推奨順序：まず 1（CC0）でUI/エフェクト/効果音を底上げ → キャラは2（本項の生成パイプライン）で対応済み。
いずれも本体コードにはほぼ影響せず、`public/assets/` の差し替えと `getCharacterSprite()` 1箇所の変更で完結した。

---

# 第2次アップデート — 拠点導線の一本化・ストーリー・難易度設計

テストプレイのフィードバック6件（①拠点設置の二重化 ②PC戦の難易度とストーリー
③ポイントの傾斜配分 ④フィールドの不足 ⑤小4向けの難易度設計 ⑥大砲が強すぎる）への対応記録。

## 8. ① 拠点設置の二重化を解消（作戦立案の廃止）

### 判明した実態
「拠点づくり」と「作戦立案」の二重どころか、戦闘中の建設を含めて**3か所**で拠点を置いていた。
さらに決定的な問題として、`computeBattleLoadout()` の戻り値は `BattleSetupScreen` にも
`BattleScene` にも props で渡されるだけで**一度も参照されていなかった**。
つまり拠点づくりで建てた施設は、戦闘に一切影響していなかった。

### 対応
- `BattleSetupScreen.tsx` を**削除**。拠点づくりを唯一の拠点編集画面にした。
- `catwars/utils/deployPlan.ts` を新設し、拠点の配置を戦場の自陣ゾーンへ**自動転写**する。
  - コアを自陣の最も奥（敵重心から最遠・ゾーン中央寄り）に置き、他の施設は
    拠点での**相対位置を保ったまま**配置。入らないものは最寄りの空きへずらす。
  - 持ち込み数は `BattleLoadout` で制限（壁 `maxWallSlots` / 防衛 `maxBuildingSlots` /
    大砲・テスラは対応施設を建てているときのみ）。**ここで初めて loadout が実際に効く**。
  - 呪文の所持数も `loadout.healCharges` / `rageCharges` から取るようにした。
- 出撃導線は **出撃！→ 範囲えらび → ステージえらび → 戦闘** の3ステップに短縮。
- 新しい `StageSelectScreen` はハブと同じ視覚言語（角丸2xl・半透明ダーク・Orbitron見出し・
  青⇄赤アクセント）で作り直し、旧「作戦立案」画面のUI不一致を解消した。
  （※この節の「自動転写」は第3次アップデートで廃止し、ステージごとに陣地を組む方式へ変更した。§16 を参照）

## 9. ②④ ストーリーキャンペーン（全8章・フィールド8種）

`catwars/data/campaign.ts` に8章を定義。章とマップは**1対1**（`catwars/data/battleMaps.ts` を
3種→8種に拡張）。各章は「戦いのタイトル／敵の名前・肩書き／敵の背景（2文）／作戦の一言／
戦術ヒント／勝利・敗北時の一言」を持ち、ステージ選択・開戦ブリーフィング・リザルトの3か所で表示する。

| 章 | 戦いのタイトル | 敵 | フィールド | 導入する新要素 |
|---|---|---|---|---|
| 1 | 月のはずれの前哨基地 | ガラクタ隊長 | ルナ・アウトポスト | 障害物なし（出す→進む→こわす） |
| 2 | クレーター峡谷のふたご | ミギー＆ヒダリー | クレーター峡谷 | ルート選択（岩で通路が2本） |
| 3 | 隕石王のなわばり | ロックニャン | ネビュラ・フロント | 迂回と遮蔽 |
| 4 | 鉄壁のヨロイニャン | ヨロイニャン | アイアン・フォートレス | 壁の要塞（こわす／飛びこえる） |
| 5 | 軌道ブリッジの番人 | ブリッジャ | オービタル・クロス | 橋＝密集リスク |
| 6 | 重力沼のグラビニャ | グラビニャ | グラビティ・マーシュ | 移動コスト（沼） |
| 7 | 星くだきアステロ | アステロ | アステロイド・ゾーン | 複合地形 |
| 8 | 銀河皇帝ニャースベイダー | ニャースベイダー | ギャラクティック・シタデル | 三重防衛＋ボスウェーブ |

進行は線形（前章クリアで次章開放）。章の進行度はハブにも進捗バーとして出る。

## 10. ⑤ 難易度設計 — エビデンスと目標値

エビデンスレベル: **1**=メタ分析・複数RCTに支えられた理論 / **2**=大規模実証研究 /
**3**=業界で広く検証された実務知見。

| # | レベル | 出典 | 設計への反映 |
|---|---|---|---|
| E1 | 1 | フロー理論 (Csikszentmihalyi 1990) / ゲーム適用 (Chen 2007, CACM) | 章ごとに「わずかに上回る」量だけ強くする |
| E2 | 1 | 自己決定理論 (Ryan & Deci 2000; Ryan, Rigby & Przybylski 2006) | 有能感＝「勝てるが楽勝ではない」を勝率目標に翻訳 |
| E3 | 1 | 発達的処理速度 (Kail 1991 メタ分析; Der & Deary 2006) | **TTK 4秒以上**という下限を反応時間から逆算 |
| E4 | 2 | Lomas, Patel, Forlizzi & Koedinger 2013, CHI (n≈3万) | 教育ゲームは"易しめ"が継続率最大 → 序盤を明確に易しく |
| E5 | 2 | Sampayo-Vargas et al. 2013, Computers & Education | 適応的難易度は有効だが効果量は中程度 → 適応は補助に留める |
| E6 | 3 | Hunicke 2005 (DDA) | 自動調整は**下方向のみ**。成功者を密かに難しくしない |
| E7 | 3 | Baldwin, Johnson & Wyeth 2014 | 隠れた調整は不信を生む → 作動中は**明示表示** |

### 勝率の目標値
第1〜2章 約85%（E4）／第3〜6章 約70%（E1・フロー帯の中心）／第7〜8章 約55%（E2・達成感のピーク）。

### 章ごとのパラメータ
`ChapterDifficulty` として数値化。敵HP・攻撃力倍率、防衛施設の威力・HP倍率、初回ウェーブ猶予、
ウェーブ間隔、1ウェーブの体数、出現する敵の種類、開始⚡、⚡自然回復。

| 章 | 防衛威力 | 初回ウェーブ | ウェーブ間隔 | 体数 | 敵の種類 | 開始⚡ |
|---|---|---|---|---|---|---|
| 1 | ×0.6 | 45秒 | 36秒 | 1 | ノラ兵 | 260 |
| 2 | ×0.7 | 40秒 | 32秒 | 1→2 | ＋スナイパー | 240 |
| 3 | ×0.8 | 35秒 | 28秒 | 2 | 同上 | 220 |
| 4 | ×0.9 | 32秒 | 25秒 | 2→3 | ＋かけぬけ兵 | 200 |
| 5 | ×1.0 | 30秒 | 22秒 | 2→3 | 同上 | 190 |
| 6 | ×1.1 | 28秒 | 20秒 | 3 | ＋重装ロボ | 180 |
| 7 | ×1.2 | 26秒 | 18秒 | 3→4 | ＋飛行ドローン | 170 |
| 8 | ×1.3 | 24秒 | 16秒 | 3→4 | ＋ボス(W4) | 180 |

旧実装は「18秒→12秒・最大3体・2種類固定」のハードコードで、章の概念自体が無かった。
また開始⚡が0・回復1/秒だったため**最初のネコを出すまで30秒待たされていた**。開始⚡の付与は
この立ち上がりの問題への対応でもある。

> ⚠️ **この表は第2次アップデート時点のもの**。その後、敵の増援は「ウェーブ（何秒おきに
> 何体）」ではなく**ポイント経済**（たまったら1体ずつ湧く）に置きかわり、数値も
> 大きく変わっている。現行値は **§41** の表を参照。

### サポートモード（下方向のみのDDA）
同じ章で2連敗→Lv1（敵の与ダメージ×0.85・ウェーブ間隔×1.2・開始⚡+60）、
4連敗→Lv2（×0.7・×1.4・+140）。**勝つと即座に解除**。E6に従い上方向の調整は一切行わず、
E7に従いステージ選択・ブリーフィング・戦闘中バーの3か所で作動を明示する。

## 11. ⑥ 大砲・テスラの難易度分析と調整

### 原因の特定（コード実測）
| # | 原因 | 実測値 |
|---|---|---|
| a | 威力過多 | 大砲 20dmg/1000ms=**20DPS** → 基本ネコ(HP60)が**3.0秒**で死亡。隠しテスラは**40DPS**→**1.5秒** |
| b | 遮蔽判定なし | `BattleScene` の索敵が直線距離のみ。壁の内外を区別せず「壁を越えて撃つ」状態 |
| c | 標的選択が事実上ランダム | `hostiles.find(...)` が**配列の先頭**を撃つ。「タンクで砲撃を引きつける」戦術が機能していなかった |

### 対応
- **(a) 数値調整** — 大砲 `14dmg/1300ms`（10.8DPS）・射程 4→3.2、
  隠しテスラ `22dmg/2000ms`（11DPS）・射程 3.5→2.6。
  テスラは「1発は重いが発射が遅い」形にして、奇襲の鋭さを残しつつ即死を防いだ。
  加えて**照準時間**（大砲0.7秒・テスラ0.25秒）を新設し、初弾の前に予告が入るようにした。
  射程を3.2にしたことで、遠距離系（射程3.5）が一方的に削れる相性が成立する。
- **(b) 遮蔽** — `catwars/utils/combatRules.ts` の `hasLineOfSight()`。壁が射線を遮る。
  防衛施設だけでなく**遠距離ユニット（射程2以上）にも同じルール**を適用。
  飛行系は例外（壁を越えて直進できるかわりに、壁のかげにも隠れられない）。
- **(c) ヘイト** — `pickDefenseTarget()` が「遮蔽なしの候補のうち、ヘイト値→距離の順」で選ぶ。
  タンク系・ボス級 3.0／重装 2.5／爆発系 1.6／その他 1.0。

### 調整後の Time-To-Kill（基本ネコ HP60・進化前）
検証スクリプトによる実測値。**全章で4秒以上**という設計下限（E3）を満たす。

| 章 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 大砲 | 9.29s | 7.96s | 6.96s | 6.19s | 5.57s | 5.06s | 4.64s | **4.29s** |
| テスラ | 9.09s | 7.79s | 6.82s | 6.06s | 5.45s | 4.96s | 4.55s | **4.20s** |

進化（Lv10でHP×1.45／Lv30でHP×2.0）が乗れば、この値はさらに伸びる。
＝「学習してネコを育てるほど戦況が楽になる」という教育的フックが数値としても成立する。

旧実装は戦闘画面に「戦術エビデンス解説」パネルを常設し、"D1 retention が統計的に上昇" 等
出典で裏づけられない主張をプレイヤーに直接見せていた。今回このパネルはゲーム内から**撤去**し、
根拠・エビデンスレベル・実測値は本ドキュメント（本節）に一本化した。

## 12. ③ ポイントの傾斜配分

### 課題
戦闘中クイズの報酬は問題によらず一律 `40⚡`、学習報酬も `12CR / 2EN / 4XP` 固定だった。
一瞬で答えられる選択肢問題と、何工程もある筆算が同じ報酬なのは不公平で、
均一報酬下では**最短時間で解ける項目だけを周回するのが合理的戦略**になってしまう。

### 設計
`catwars/data/problemWeights.ts`。**報酬 = 基準値 × 重み**、重みは 0.6〜2.0 にクランプ。

- **難易度係数**: 既存の `difficultyMap`（全96サブトピックに1〜5を付与ずみ）を再利用。
  新しい難易度表を作らないことで、カードバトルの `calcDamage` と難易度観を共有する（単一ソース）。
  1段階あたり +0.14 のゆるやかな傾斜（難度1と5で約1.65倍）。
- **所要工程係数**: `guided`（筆算シミュレーター等の多工程）×1.45 / 図つき ×1.12 /
  選択肢（即答）×0.78 / その他 ×1.0。

エビデンス: 認知負荷理論の要素相互作用性（レベル2: Sweller 1988; Sweller et al. 2011）、
努力割引による回避行動（レベル2: Botvinick et al. 2009）、
過剰正当化効果を避ける「報酬 ∝ 学習上の価値」の原則（レベル3: Deci, Koestner & Ryan 1999）。

### 実測（基準34⚡の戦闘中クイズ）
| 問題 | 重み | 獲得⚡ |
|---|---|---|
| 同じ分母の大小（難度1・選択肢） | 0.67 | 23 |
| 九九のはんい（難度1・記述） | 0.86 | 29 |
| 分数のひき算の文章題（難度4） | 1.14 | 39 |
| L字型の面積（難度4・図つき） | 1.43 | 49 |
| 3けた÷1けた（難度3・guided筆算） | 1.65 | 56 |
| くり下がりのあるひき算（難度5・guided） | 2.00 | 68 |

約3倍の開き。獲得量と「てごたえボーナス／むずかしい問題ボーナス」のラベルは出題中に表示し、
なぜ多くもらえるのかが子どもに伝わるようにした。

学習側（`bridge.ts`）は `recordAttempt` から単元名しか渡ってこないため、難易度係数のみを適用する
（難易度3を1.0の基準とし、難易度1で約0.75倍、難易度5で約1.25倍）。既存の MP システムは無変更。

## 13. 検証

- `npx tsc --noEmit` / `npm run build` ともにエラーなし。
- ヘッドレスChromiumで実画面を操作し、ハブ→範囲えらび→ステージえらび→ブリーフィング→戦闘→
  戦闘中クイズ まで通しで確認。
- 純粋関数を実データで検証（`hasLineOfSight` の遮蔽4ケース、`pickDefenseTarget` のヘイト・遮蔽・
  飛行3ケース、全8マップでの拠点自動転写＝コア配置成功・重なりなし・ゾーン外なし、
  敵拠点の自己重なりと通行不可地形上への設置がゼロ、全章のTTK、報酬の重み）。

---

# 第3次アップデート — 横長フィールド・当日リセット制の陣地・ステージギミック・体型差

テストプレイでの指摘（拠点が持ち越せていない／フィールドが狭い／iPad横向きの表示／
ギミックが欲しい／キャラの体格が全部同じ）への対応記録。

## 14. 盤面を横長 28×16 に変更

iPad を横に持ってプレイする前提なのに、盤面は 15×15 の正方形だった。
1180×820 での実測では **盤面600×600 に対し表示領域は505px しかなく、下95px（自陣コア付近）が
画面外**。さらに横幅1180のうち580pxが未使用だった。

- `GRID_SIZE = 15` を `GRID_W = 28` / `GRID_H = 16`（7:4）に置き換え、全8マップを描き直した。
- `PinchZoomLayer` に `contentSize` を渡すと**表示領域に自動フィット**するようにした。
  `ResizeObserver` で画面回転にも追従する。⟳ ボタンも「1倍」ではなく「ぴったり収まる倍率」に戻る。
- A* の探索上限を 500/600 → 1600/1800 に引き上げた（マス数が 225 → 448 に増えたため）。
- 実測（1180×820）: 盤面 1103×630・上端70px・下端701px、下部バー上端707px → **切れずに収まる**。

## 15. リザルトが下部バーに隠れる問題

リザルトのモーダル（`absolute z-50`）と下部の出撃バー（`relative z-50`）が**同じ z-index の
兄弟要素**で、DOM順が後のバーが手前に描画されていた。モーダルを `fixed z-[160]` に変更。
あわせて下部バーの行数を 4行 → 2行に圧縮し、高さを **251px → 113px** にした
（補充ボタンと建設ボタンを魔法カード行へ統合）。

## 16. 陣地は「その日ぶんを毎回組む」方式へ

### 問題
前回の「拠点の配置を戦場へ自動転写する」実装は、**拠点グリッド15×15 に対して自陣ゾーンが
4〜6マス幅**しかなく、コアから離れた施設が入りきらずフォールバックで再配置されていた。
実測でも `大砲(-2,-2)` → `大砲(0,-2)` のように相対位置が崩れていた。
ゾーンが拠点より狭い限り、1:1 の持ち越しは原理的に不可能だった。

### 新しいモデル
- **ステージを決めてから陣地を組む**。出撃導線は
  **出撃！→ 範囲えらび → ステージえらび → 陣地づくり → 戦闘**。
  陣地はその章の自陣ゾーンの形そのままに組むので、**置いたものがそのまま戦場に出る**（ずれゼロ）。
- **自陣ゾーンの広さは章ごとに変える**。広い章と狭い章を交互に置き、毎回ちがう組み立てを
  考えさせる（ひとつの必勝パターンで流せないようにする）。

  | 章 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
  |---|---|---|---|---|---|---|---|---|
  | 自陣の幅 | 10 | 6 | 10 | **4** | 8 | 10 | 5 | 8 |

- **施設は当日リセット制**（`catwars/store/useBaseStore.ts`）。
  - 支払いは **⚡エナジー**（当日リセット通貨＝今日解いたぶん）。壁3 / キャンプ10 / 兵舎10 /
    金山8 / 大砲12 / テスラ18。
  - 建てた施設は「今日のもちもの」として、その日のうちは**どのステージでも自由に置き直せる**（無料）。
  - 日付が変わると もちもの も 配置 もリセットされる。
  - こわすと ⚡ が全額もどる（組み替えを気軽にできるように）。

  **なぜ所有制（買えば永久）にしなかったか**: 💠は貯まり続けるため、数週間で
  「その日に勉強しなくても満額の陣地が組める」状態になり、毎日学習させる目的が崩れる。
  当日リセットなら「今日解いたぶん＝今日の陣地の強さ」が直結する。
  失うのは当日ぶんだけなので、1日休んでも進行そのものは失われない（損失忌避で縛りすぎない）。

- `computeBattleLoadout()` は「今日建てた施設の数」を受け取る形に変更。兵舎を建てた日は
  その解放ぶんの兵種が**無料で使える**ようにし、施設を建てる意味を戦闘に直結させた。
  キャンプはヒール／レイジの所持数を増やす。

## 17. ステージギミック（`catwars/utils/stageHazards.ts`）

第1〜2章は素の盤面のままにして基本操作に集中させ、第3章から段階的に導入する。
どれも**敵・味方・中立すべてに等しく作用**する（一方だけ損をする仕掛けは理不尽に感じられるため）。

| ギミック | 内容 | 登場 |
|---|---|---|
| 溶岩 (LAVA) | 乗っているあいだ 14ダメージ/秒。通行は可能。経路コストを4にして自然に迂回させる。飛行系は無傷 | 3・7・8章 |
| 流星 | 予告円が出てから **1.6〜1.8秒後**に着弾し範囲ダメージ。見てから逃がせる猶予を必ず置く | 4・7・8章 |
| 中立エイリアン 👾 | 巣から定期的に湧く第3勢力。陣営に関係なく最も近い相手を襲い、両陣営の防衛施設からも撃たれる | 5・7・8章 |
| 巨大生物 🦑 | 決まった経路を往復し、進路に入ったキャラを攻撃。A*ではなく直線往復にして「いつ通るか」を読めるようにした | 6・8章 |

`BattleEntity.team` に `'NEUTRAL'` を追加し、防衛施設の索敵対象にも中立を含めた。

## 18. キャラクターの体型差と役割特性

### 体型
`CharacterFamily` に `bodySize`（戦場での表示px・1マス=40px）と `bodyType`（体型の指定文）を追加。
描画はマスの左上ではなく**中心ぞろえ**に変更し、大型が右下にずれないようにした。

| 系統 | 表示サイズ | 体型 |
|---|---|---|
| 高速系 | 26px | とても小さく細い |
| 遠距離系 / 回復系 | 30px | すらりと細身 / 小柄で丸い |
| 魔法系 | 32px | 小柄で猫背 |
| 近接系 | 34px | がっしりした標準体型 |
| 爆発系 | 36px | ずんぐりして丸い |
| 飛行系 | 38px | 胴は細いが翼が大きい |
| タンク系 | **58px** | ずんぐりと巨大（マスをはみ出す） |
| ボス3種 | **86〜96px** | 圧倒的に大きく特異なシルエット |
| 巨大生物 | **110px** | 盤面を横切る中立の巨体 |

スプライトも33体すべて再生成した。系統ごとに `BODY SHAPE` と `SIZE` をプロンプトへ明示し
（タンク=「小さな頭に対し極端に太い腕と胴」、高速系=「最も小さく細い」、
ボス=「画面いっぱいの巨体」）、共通スタイル文で画風のブレを抑えている。
生成は Pollinations.ai（無料・キー不要）＋ 自作の背景除去（四隅の色からフラッドフィル →
バウンディングボックスでトリム → 256×256 に正規化）。

### 防衛施設ダメージ耐性（`defenseResist`）
「タンクは大砲に強い」を、HPを増やすのではなく**役割ごとの軽減率**として表現した。
HP増強と違い「大砲に強い」という個性がはっきり出て、ヘイト管理と噛み合う。

| 系統 | 軽減率 |
|---|---|
| タンク系 | **45%軽減** |
| ボス（ハイドラ / ベイダー / カイザー） | 40% / 30% / 25% |
| 爆発系 | 15%軽減 |
| 近接系・飛行系・回復系 | 0% |
| 遠距離系・魔法系 | 10%**増**（打たれ弱い） |
| 高速系 | 20%**増**（紙装甲だが速い） |

## 19. そのほかの修正

- **水路が透明で見えていなかった**（既存の不具合）。`TerrainLayer` が CSS クラス
  `.terrain-water` に依存していたが、そのクラスはどこにも定義されていなかった。インラインで描画。
- 陣地づくりで自動配置したコアがストアへ保存されず、何も編集せずに出撃すると
  **コアが戦場に出ない**不具合を修正。

## 20. 検証

- `npx tsc --noEmit` / `npm run build` ともにエラーなし。
- iPad 横向き（1180×820）で通し操作: ハブ → 範囲えらび → ステージえらび → 陣地づくり →
  ブリーフィング → 戦闘。盤面が切れずに収まること、下部バーが113pxに収まることを実測。
- ギミックをブラウザ上でポーリング検証: 第4章で流星の予告円を2件検出、
  第5章で中立エイリアンの出現と敵ウェーブを検出、第6章で巨大生物の描画を確認。
  いずれも JS エラーなし。

---

# 第4次アップデート — キャラの怖さ軽減・施設名の明確化・ギミックのリッチ化

テストプレイでの指摘3件（キャラが怖い／砲台・テスラの見た目と名前が分かりにくい／
ステージギミックがスタンプのようでチープ）への対応記録。

## 21. キャラクターの怖さ軽減（33体＋中立2体を再生成）

生成プロンプトの `STYLE` に「大きく丸くうるうるした目」「暖かく親しみやすい表情」を
最優先の指示として追加し、`NEG`（除外指定）にも「怖い顔・赤く光る目・鋭い牙・
暗く重い雰囲気」を明示的に加えた。ゲームバランス上の設定（`bodySize`・`defenseResist`・
HP・ダメージ等）は無変更で、見た目のみを差し替えている。

特にボス3種は「怖さ」の指摘の中心だったため、シルエットの迫力（大きさ・装備・
役割アイデンティティ）は保ちつつ、配色と表情を大きく軟化した。

| ボス | 変更前 | 変更後 |
|---|---|---|
| boss-titan | 深紫の宇宙ホラー、多頭・触手、"terrifying and imposing" | パステルラベンダーのぷにぷにしたコズミックジェリー、丸い瞳 |
| boss-artillery | 黒い単眼センサー、金属サイボーグ | 丸い瞳の親しみやすいロボット、暖色のオレンジ・ゴールド |
| boss-overlord | 黒一色のマント、赤く光る目、"towering and menacing" | 藍色×銀の星柄マント、丸い琥珀色の瞳、シアンの光る剣 |

## 22. 施設名とイラストの改善

| BuildingType | 旧名 | 新名 | 変更理由 |
|---|---|---|---|
| CANNON | 大砲 | **砲台** | イラストが手持ちレーザー銃に見えるとの指摘。据え置き型であることを名前でも明示 |
| HIDDEN_TESLA | 隠しテスラ | **電撃トラップ** | 名前だけでは何の施設か伝わらないとの指摘 |

`BuildingType` 列挙値自体（内部キー）は互換のため無変更。表示名・説明文・
プレイヤー向けテキスト（`campaign.ts` のヒント／ブリーフィング、`StageSelectScreen`）を更新した。

全7種の施設イラストを `gen-buildings.mjs` で再生成。プロンプトに
「fixed base/turret standing on the ground」「no hand grip, no handheld weapon」を
明示し、丸みを帯びたゆるキャラ世界観に統一した。砲台は「据え置きのタレット」、
電撃トラップは「地面の丸いハッチから光るコイルが飛び出す罠」として、
役割が一目で伝わる意匠に描き直している。

## 23. ステージギミックのリッチ化

流星を絵文字1個（☄️）から、多層エフェクトの組み合わせに作り直した（`BattleScene.tsx`）。

- **予告フェーズ**: レーダー走査線が回転するターゲットサークル ＋ 残り時間ぶんだけ光の弧が
  閉じていくカウントダウンリング（`conic-gradient`）＋ 尾を引きながら目標へ接近する彗星
  （画面外から予告時間ぶんかけて実際に飛来する、静止した記号ではなく軌道のある演出）。
- **着弾フェーズ**: フラッシュ ＋ 二重の衝撃波リング ＋ 8方向への放射状デブリ ＋
  じわっと消えるこげ跡（0.9秒）。

中立勢力（エイリアン・巨大生物）も絵文字（👾／🦑）から専用スプライトに差し替えた。
`gen-sprites.mjs` に `NEUTRAL_ROSTER` を追加し、ネコとは別の「ぷにぷにした宇宙生物」
（毛皮ではなくゼリー質、丸い大きな瞳）として描き分けている。戦場では浮遊／呼吸の
アニメーションと発光レイヤーを重ね、静止画のスタンプ感を避けた。

## 24. 検証

- `npx tsc --noEmit` / `npm run build` ともにエラーなし。
- ヘッドレスChromiumで実戦闘を操作し、陣地づくり画面で「砲台」「電撃トラップ」の
  新名称が表示されること、戦闘中に流星のレーダー照準・落下する彗星・着弾の衝撃波が
  実際に描画されることを確認（JSエラーなし）。
- 生成した全35枚（キャラ33＋中立2）・施設7枚をサムネイルで目視確認。

---

# 第5次アップデート — サンリオ風フラットデザインへの全面変更・砲台のレールガン化

「目は丸くなって良い感じだが、それ以外がリアルすぎて気持ち悪い」という指摘への対応。

## 25. スタイル方針の転換（絵画的リアリズム → フラットなマスコットデザイン）

第4次アップデートの「目を丸く」路線は方向性としては正しかったが、それ以外の要素
（陰影のグラデーション・毛並みの質感・光沢のあるハイライト）が絵画的・写実寄りのまま
残っており、結果として「丸い目のリアルな猫」という一貫性のない気味悪さを生んでいた。

生成プロンプトをサンリオキャラクター（ハローキティ・シナモロール・マイメロディ等）を
明示的な参照点とする内容に全面書き換えした。

- **STYLE**: 「flat 2D vector mascot design, Sanrio-style」を先頭に置き、
  「EXTREMELY SIMPLE」「ZERO gradient shading」「ZERO fur texture」「ZERO realistic
  anatomy」を明示。目は「huge simple round eyes（単色の丸・白ハイライト1点のみ）」に単純化。
- **NEG**: 「no painterly digital art」「no semi-realistic shading」「no gradient
  shading」「no detailed fur or hair texture」等を追加し、旧来の絵画的レンダリングを除外。

boss-titan の「複数の頭＋触手」という**設定は変更していない**が、解剖学的に生々しく
見えないよう「丸い風船状の単一の塊＋簡略化した突起」として描くよう指示を調整した
（グロテスクな表現を避けつつ、既存の役割・ステータス設定は維持する方針）。

タンク系（stage2/3）は「golem/boulders」という質感を誘発する語が眉をひそめた表情と
ざらついた筋肉の描き込みを引き起こしていたため、「NO fur texture」「no eyebrows」
「flat solid color」を明示して単一系統内でも均質なフラットさになるよう再調整した。

全33体＋中立2体（エイリアン・巨大生物）を再生成。

## 26. 砲台をレールガンに変更

「レーザー銃 → 据え置き砲台」で一段階改善したのち、「レールガンにしてほしい」との
追加要望を受け、単発の砲身ではなく**2本の平行なレールに荷電粒子（シアンの光る弾）が
走る**兵器として再生成した。名称「砲台」・ゲームバランス（ダメージ・射程等）は無変更。

## 27. 検証

- `npx tsc --noEmit` / `npm run build` ともにエラーなし。
- 全35枚のサムネイルを目視確認。タンク系は初回生成でやや荒い質感・不機嫌な表情が
  残っていたため、プロンプトを調整して再生成し直した（1回の目視レビューで
  スタイルの逸脱を検知し補正するワークフローを実施）。
- ヘッドレスChromiumで戦場に複数系統（高速・近接・タンク・ボス）を実際に配置し、
  体格差を保ったままフラットデザインが反映されていることを確認。

---

# 第6次アップデート — ハイドラニャンの透過バグ・5系統のデザイン刷新・ひらがな表記統一

細かい調整3件（背景透過バグ・デザインの質にばらつき・カタカナ表記）への対応記録。

## 28. ハイドラニャンの背景透過バグ

ハイドラニャン(boss-titan)は本体が白系の配色のため、生成時の白背景とほぼ
同色になり、四隅サンプリング＋フラッドフィルによる背景除去が本体の大部分
まで透過させてしまっていた（実測: stage1で内側59.8%・stage3で79.7%が透明化）。

**対策（`gen-sprites.mjs`）:**
- 生成プロンプトの背景指定を「白」から「キャラのどの配色にも使われない
  中間グレー」に変更。
- 背景色推定を四隅4点から外周の多点サンプリング＋中央値に変更（角にキャラの
  一部がかかる誤検出を軽減）。
- 除去後に「不透明の外接矩形の内側」の透過率を計測し、18%を超えたら
  「本体を巻き込んだ」と判定して例外を投げる安全策を追加。呼び出し側
  (`generateOne`)でこれを検知すると、シードを変えて自動的に再取得する
  （最大2回リトライ）。この安全策は実際に ranged-3 / magic-1〜3 / speed-1 /
  flying-1,2,3 で作動し、正しい絵柄を得るまで自動的に取り直した。

## 29. 5系統のデザイン刷新（近接・遠距離・高速・飛行・魔法）

「ぼむにゃー／ニャース／ニャイアントは丸っこく二頭身でシンプルなのが良い」
「ニャーチャーはこの宇宙戦争のイメージに合わない」「コニャウモリ／ニャブリン／
ニャバリアン／ミニャジョも同様に足りない」という指摘を受け、5系統の
`identity`/`body` を全面的に書き直した。

**問題点**: これらは中世ファンタジーの人型直立ポーズ（剣を構える・弓を引く・
短剣を持つ）が中心で、丸いブロブ体型でも宇宙SFの世界観でもなかった。

**対応**: 装備を宇宙SFモチーフに置き換えつつ、体型を「丸いブロブ・最小限の
把持」（tank/bomber/healerと同じ基準）に統一した。

| 系統 | 旧装備（中世ファンタジー） | 新装備（宇宙SF） |
|---|---|---|
| 近接 | 剣を構える直立戦士 | 体に沿わせた小さな発光ブレード |
| 遠距離 | フード＋弓矢＋矢筒 | 小さな丸いブラスター（進化で二丁→大型砲） |
| 高速 | 短剣＋スカーフの盗賊 | 星形のスピードスパークル航跡 |
| 飛行 | コウモリの翼（素のまま） | 丸いロケット風フィン翼 |
| 魔法 | 長いローブ＋杖 | 短い丸フード＋触れているホロオーブ |

浮遊オブジェクト（オーブ・砲）が本体から離れていると背景除去の穴あき
検知に頻繁に引っかかったため、「本体に触れさせる（flush against, no gap）」
という指示を加えて安定させた。

デザイン変更に伴い、フレーバーテキストも新装備に合わせて更新した
（例: 「弓矢で遠くから」→「小さなビーム銃で遠くから」、「二刀流のアサシン」→
「エネルギー弾を操るアサシン」、「手裏剣を投げる忍者」→「きらめく軌跡を残す忍者」）。

## 30. ぼむにゃーの目の非対称を修正

stage1で左右の目の大きさ・形が非対称になる事故が出ていたため、identityに
「両目とも同じ大きさの左右対称な丸形・ウインクしない」を明示して再生成した。

## 31. キャラクター名表記をひらがなに統一

33体の進化フォーム名、およびボス3系統のdisplayNameをカタカナからひらがな
表記に変更した（例: ニャバリアン→にゃばりあん、コズミック・ハイドラ→
こずみっくはいどら）。中黒(・)は自然な読みになるよう除去して連結した。
`campaign.ts` のヒント文中で個体名を参照している箇所も合わせて更新した。
役割説明文・フレーバーテキスト中の一般的な外来語（センサー・サイボーグ等）
は対象外とし、固有の「キャラクター名」表記のみを変更している。

## 32. 検証

- `npx tsc --noEmit` / `npm run build` ともにエラーなし。
- 全対象スプライトを目視確認（ハイドラニャン3段階の欠け消失、5系統×3段階の
  丸み・宇宙SF感、ぼむにゃーの目の左右対称性）。
- ヘッドレスChromiumでネコ図鑑画面を確認し、新デザイン・ひらがな名が
  実際のUIで正しく表示されることを確認した。

## 33. 敵の増援ポイント経済への刷新（①のフィードバック対応）

テストプレイで「敵の出現が遅すぎる」「金山の場所からしか出ていない印象」
という指摘を受けた。実測すると根拠のある不具合だった。

- 旧実装（`waveIntervalMs`/`firstWaveDelayMs`）は 第1章で **初弾45秒・以降36秒間隔**、
  1回のウェーブが1〜4体というバッチ湧きだった。一方プレイヤー側は
  passive収入(1.15〜1.6⚡/秒)に加え、正解1回で23〜68⚡が即座に入る
  (`catwars/data/problemWeights.ts`)。基本ユニットのコストは30〜160⚡なので、
  プレイヤーは 数秒〜十数秒に1体のペースでネコを出せるのに対し、敵は
  最大36秒に1回しか動きがない。これが「手ごたえがない」「静止して見える」
  という体感の直接原因だった。
- 出現位置も `spawnBaseX = Math.max(defenderBldgs.map(b => b.x))`,
  `spawnBaseY = 平均y` という **固定1点** から出しており、章によっては
  金山(GOLD_MINE)のすぐそばになる配置だったため「金山から出ている」ように
  見えていた。

対応（`catwars/data/campaign.ts`, `catwars/components/game/BattleScene.tsx`）:

- プレイヤーと同じ「たまったらすぐ出す」経済に統一。`ChapterDifficulty` に
  `enemySpawnRatePerSec`(毎秒たまるポイント) と `firstSpawnDelayMs`(初弾までの
  猶予) を追加し、`waveIntervalMs`/`waveSize`/`waveSizeMax`/`bossAtWave` は廃止。
- 各 `EnemyUnitKind` に `ENEMY_UNIT_COST`（grunt 38 / shooter 44 / runner 34 /
  brute 105 / flyer 68）を設定。次に出す1体を `pickWeightedEnemyUnit()` で
  コストの軽いキャラほど出やすい重み付き抽選であらかじめ決めておき、
  「コスト ÷ enemySpawnRatePerSec」で実際の待ち時間を決める。重いユニットは
  自然と出現間隔が長くなり、「時々つよいのが混ざる」ペースになる。
- 初弾までの猶予を 第1章45秒→**11秒**、第8章24秒→**6.5秒** に短縮。以降の
  間隔も 第1章の基本ユニットで約9.5秒、第8章で約4.5秒（brute等の重量級は
  10秒台）と、章が進むほど速く・重くなる。
- 出現位置は、自陣建物群でいちばん奥(x最大)の1マス右の列から、通行可能な
  y座標を**毎回ランダムに**選ぶよう変更（`obstaclesRef` で壁・地形を除外）。
  同じ湧き穴に固定されず、複数の場所から迫ってくるように見える。
- ⑤のサポートモード（`effectiveDifficulty`）は `enemySpawnRatePerSec` を
  `waveIntervalMult` で割ることで従来どおり「連敗が続くと敵が来にくくなる」
  効果を維持している。
- ボス出現は `bossAtSpawnCount`（通常湧きの何体め）に置き換え、経済とは
  別枠の単発トリガーとして扱う（第8章は9体め＝ラスト直前で登場）。

なお、この変更は敵の**頻度**を主に直したもので、1体あたりの強さ
（`enemyHpMult`/`enemyDamageMult`、⑥で調整した砲台・電撃トラップのDPS）は
据え置きである。頻度が上がった分だけ体感難易度も上がるはずなので、
章1〜2で85%前後の初回クリア率という⑤の目標値からズレがないか、実際の
プレイフィードバックを見て追って調整する。

## 34. 敵拠点の壁の抜け穴を修正（②のフィードバック対応）

②「CPUも様々な施設を効果的に配置してほしい」を受けて全8マップの
`enemyBase` を確認したところ、砲台・電撃トラップ・壁による段階的な防衛は
すでに章ごとに構築されていた（第1章は素の拠点、第4章で電撃トラップ初登場、
第7〜8章は多重防衛）。一方で壁の配置に実務的な不備が見つかった。

- 第2章・第3章・第5章: タウンホール(2×2)の片側だけに3〜4マスの壁が
  あり、上下どちらかに1マスの隙間があった（例: 第5章は壁が(23,6)(23,9)の
  2マスだけで中央(y7,8)が素通り可能だった）。
- 第6章: 壁が(23,6)(23,7)(23,9)で **(23,8)が抜けていた**。タウンホールの
  真横がノーガードという設計ミス。

いずれも、タウンホールの上下1マスずつを含む連続した壁（y5-9または
y6-9の4〜5マス）に統一した。まっすぐ来る部隊は壁で止め、大回りする
部隊は既存の外周砲台（第2・3章は(19,3)(19,12)や(22,3)(22,12)、第5章は
(22,7)の砲台＋(21,10)の電撃トラップ）が咎める形は維持している。
建物どうしの座標重複がないことはスクリプトで確認済み。

## 35. 移動命令が効かない不具合を修正（④のフィードバック対応）

「キャラに移動のコマンドを使うとうまく移動しない」という報告を受けて
`BattleScene.tsx` の戦闘ループを確認したところ、実際に一歩も動かない
不具合だった。

原因: ユニットの移動処理は `if (射程内) {攻撃} else if (HOLD) {} else if
(MOVE_TO) {…} else {…通常戦闘移動…}` という if/else 分岐になっており、
実際に座標を進める「Vectors apply stepper」（`entity.path` にそって
`entity.x/y` を加算する処理）は **最後の `else`（通常戦闘移動）ブランチの
中にしか書かれていなかった**。MOVE_TO ブランチは `findPathWithTerrain()`
で経路を計算して `entity.path` にセットするところまでは正しく動いていたが、
そこで分岐が終わってしまい、stepper に到達できないため経路だけ決まって
一歌も進まない状態になっていた（2フレーム目以降は `entity.path.length > 0`
なので再計算もされず、完全に停止する）。

修正: MOVE_TO の経路決定と通常戦闘移動の経路決定を、共通の`else`ブロック
内のif/elseに統合し、どちらの場合も同じ stepper（経路にそった移動＋
味方どうしの分離）を必ず通るようにした。あわせて、移動中に射程内へ
入った敵は最優先で攻撃される（`bestTarget`は移動命令と無関係に全体最寄り
の敵を毎フレーム再評価しているため）ことを確認し、「指定地点へ最短距離で
移動し、近くの敵は攻撃する」という要望どおりの挙動になった。

## 36. 敵もキャンプ・兵舎を建てて増援を出す（①の追加フィードバック対応）

§33 の対応後、「敵は金山で稼いだポイントだけで戦力を出しているような
印象。敵キャラもキャンプや兵舎をつくって、そこからも出るようにしてほしい」
という追加の指摘を受けた。実際、旧実装では敵の増援は章ごとの固定レートで
湧くだけで、**敵拠点にキャンプも兵舎も置かれていなかった**（`enemyBase` は
コア・砲台・電撃トラップ・壁・金山のみ）。プレイヤー側だけが陣地づくりで
キャンプ・兵舎を建てる非対称な作りになっていた。

対応（`catwars/data/campaign.ts`, `battleMaps.ts`, `BattleScene.tsx`）:

- 全8マップの `enemyBase` に **アーミーキャンプ（2×2）と兵舎（1×1）** を追加。
  章が進むほど数が増える（第1章はキャンプ1つ → 第8章はキャンプ2＋兵舎2）。
- `ENEMY_PRODUCTION_RATE` を新設し、増援ポイントの速さを
  **コアの基本値＋生きている生産施設の寄与** に変更した。
  キャンプ +1.2/秒、兵舎 +1.0/秒、金山 +0.5/秒。
  各章の `enemySpawnRatePerSec` は「全施設健在なら §33 と同じ合計値になる」
  ように基本値へ割り戻したので、**難易度の基準線は変えていない**。
- 増援の**湧き口**をキャンプ・兵舎のとなりに変更（`ENEMY_SPAWN_BUILDINGS`）。
  施設の周囲からプレイヤー側を優先して空きマスを探して出す。
  生産施設をすべて破壊した場合のみ、従来どおり敵陣最奥の列から出す。
- **兵舎が全滅すると重量級（重装ロボ・飛行ドローン）が出せなくなる**
  （`BARRACKS_ONLY_UNITS`）。第6〜8章で効く。
- 生産施設を破壊したときに、効果を明示するメッセージを出すようにした
  （「🏭 敵のアーミーキャンプを破壊！ この場所から増援が出てこなくなった！」）。
  ②のねらいである「工夫して勝つ」を成立させるには、**因果が見えること**が
  必須のため。

結果として、プレイヤーの攻め方が敵の増援の「速さ・質・位置」の3つすべてに
効くようになった。実測値（ノラ兵1体あたりの出現間隔）:

| 章 | 全施設健在 | キャンプ・兵舎を全壊 | 生産施設を全壊 |
|---|---|---|---|
| 1 | 9.5s | 13.6s | 21.1s |
| 2 | 8.3s | 15.8s | 20.0s |
| 3 | 7.3s | 12.7s | 15.2s |
| 4 | 6.6s | 10.6s | 12.3s |
| 5 | 5.9s | 11.9s | 14.1s |
| 6 | 5.4s | 10.0s | 11.5s |
| 7 | 5.0s | 9.0s | 10.3s |
| 8 | 4.5s | 9.3s | 12.3s |

### あわせて修正した勝利条件の不備

勝利判定が `!敵コア健在 && 敵の建物が1つ以上生存` という条件になっており、
**コアを最後に破壊した場合（＝全施設が同時に0になる瞬間）に勝利が成立しない**
状態だった。敵の生産施設が増えたことで「先に周りをつぶしてからコア」という
順序が現実的になり、この不備を踏みやすくなるため、開幕時に敵拠点が存在したか
を記録する `hadEnemyBuildingsRef` を使う形に直した。

## 37. PvP（ロックステップ同期）の設計指針

「FirebaseをつないでPVP対戦モードを追加した場合に問題なく動作するか」
という質問に対する回答と、実装する場合のベストプラクティスを
`docs/PVP_LOCKSTEP.md` に別ドキュメントとしてまとめた。

要点のみ:

- CAT-WARS は入力が離散的なコマンドなので、構造的にロックステップ向き。
  同期すべきは座標ではなくコマンド。
- ただし現状の `BattleScene.tsx` は決定論的ではない。実測で `Date.now()` 18箇所、
  `Math.random()` 1箇所、`Math.pow`/`hypot`/`atan2`/`cos`/`sin`（いずれも
  ECMAScript 仕様上 implementation-approximated ＝ 実装依存の近似）が計32箇所、
  および `requestAnimationFrame` 駆動の可変タイムステップがある。
- このうち**可変タイムステップは単体プレイでも実バグ**で、120Hz の端末では
  60Hz の端末の2倍の速さでゲームが進む（`moveStep = moveSpeed * 0.013` が
  「1フレームあたり」の量として書かれているため）。
- したがって移行計画は「決定論化（段階1〜4）→ 通信（段階5〜7）」の順とし、
  前半は PvP を作らない判断をしても単体プレイの改善として価値が残るよう並べた。
- より安価な代替として、相手の陣地構成だけを保存して自端末で攻略する
  **非同期PvP**（Clash of Clans 方式）も併記した。

## 38. 検証

- `npx tsc --noEmit` / `npm run build` ともにエラーなし。
- 全8マップの `enemyBase` に座標重複がないことをスクリプトで確認。
- ヘッドレスChromiumで実際に第1章を戦闘開始し、以下を確認:
  - 出撃直後にネコを1体出し、「📍移動先タップ」で離れた地点へ移動命令を
    発令。以後2秒おきの連続スクリーンショットで、ユニットが命令線
    (点線)にそって実際に移動し続けることを確認（修正前は停止していた）。
  - 第1章で戦闘開始から約13秒後（新しい `firstSpawnDelayMs=11000ms` の
    設計どおり）に「⚠️敵の増援（ノラ兵）が出現！」のトリガーメッセージと
    実体が出現することを確認（旧実装は45秒待たないと出現しなかった）。
- §36 の追加検証:
  - 全8マップについて、施設の**占有面積**（キャンプは2×2）で重なり・
    グリッド外・通行不可地形への設置がないことをスクリプトで確認。
  - 全20箇所の湧き口それぞれについて、周囲に出撃可能な空きマスが
    最低4マスあることを確認（袋小路になっていない）。
  - 各章の増援間隔を、全施設健在／キャンプ・兵舎全壊／生産施設全壊の
    3条件で算出し、破壊がきちんと増援を遅らせることを数値で確認（上表）。
  - 兵舎全滅時の unitPool を全章について算出し、第6〜8章で重量級が
    正しく封じられることを確認。
  - ヘッドレスChromiumで第1章を実プレイし、敵拠点にアーミーキャンプが
    描画されること、増援がそのキャンプの真横から出現すること、および
    「敵のアーミーキャンプから 増援（ノラ兵）が出てきた！」のメッセージが
    表示されることをスクリーンショットで確認した。

## 39. PvP（ロックステップ同期）の実装

§37 で設計指針をまとめた PvP について、**段階1〜7をすべて実装した**。
詳細は `docs/PVP_LOCKSTEP.md` の「実装状況」節を参照。

要点:

- 戦闘ロジックを React から切り離し、`catwars/sim/` の純粋関数
  `simulateTick(state, cfg, statics, commands)` に移した。
  「同じ設定＋同じコマンド列 → 同じ結果」が保証される。
- ネットワークに流すのは**コマンドだけ**（座標やHPは一切送らない）。
  `catwars/net/lockstep.ts` が入力遅延・tickバリア・デシンク検出を担う。
- PvE と PvP は同じコードを通る。違いは「P2の行動をAIが決めるか、
  ネットワーク越しのコマンドが決めるか」だけ。結果としてソロプレイも
  リプレイ・自動テストの対象になった。
- `BattleScene.tsx` は 2389行 → 1559行になり、ゲーム状態を一切持たない
  「描画と入力だけの殻」になった。

### あわせて直った既存バグ

固定タイムステップ化にともない、**単体プレイに存在していた実バグ**も解消した。

1. **フレームレート依存**: 移動量が `moveSpeed * 0.013`（1フレームあたり）
   だったため、120Hz の端末では 60Hz の端末のちょうど2倍の速さでゲームが
   進んでいた。現在は実時間で 1tick = 50ms に固定されている。
2. **A* が最短を返さないことがあった**: オープンリスト内しか探索せず、
   より短い経路を見つけても優先度キューを更新していなかった。
   `gScore` マップで管理する正しい実装に修正（④の「最短距離で移動」の要件に対応）。

### 検証

- `npm run test:sim` … 決定論テスト15項目すべて成功
  - 同一seed＋同一コマンド列で全2400tickのチェックサムが一致
  - 60fps / 120fps / 5〜120msの不規則な刻み で結果が一致
  - 2インスタンスのロックステップが全600tickで一致（RTT300ms相当でも）
  - 注入したズレ（HP -0.5）をチェックサムが検出
  - `catwars/sim/` に実装依存の数学関数が無いことを静的検査
- `npx tsc --noEmit` / `npm run build` … エラーなし
- ヘッドレスChromiumで第1章を実プレイし、JSエラーなしで
  出撃 → 敵増援（キャンプから）→ スケルトン覚醒 → 施設破壊 → 勝利・報酬
  まで通ることを確認。移動命令も目的地へ正しく近づくことを座標で確認。

### PvPを実際に遊べるようにするために残っていること

シミュレーションと通信層は完成しているが、**対戦導線のUIは未実装**。
`docs/PVP_LOCKSTEP.md` 末尾に手順を5項目で記載した（Realtime Database の
有効化、ハブへの導線追加、`LockstepSession` への差し替え、待機・デシンク
表示など）。いずれも既存の仕組みの組み合わせで足りる。

## 40. PvP対戦導線の実装（たいせん）

§39 でシミュレーションと通信層を作ったので、実際に人と対戦できる導線を実装した。
CAT-WARSハブに「🤝 たいせん」ボタンを追加し、ロビー → 相手待ち → 対戦まで通る。

- `catwars/net/matchmaking.ts` … Firestore `catwars_rooms` で部屋を管理。
  既存のPvP（card_battle / speed_duel）は `rooms` を使っているので干渉しない。
- `catwars/components/pvp/CatWarsPvpScreen.tsx` … ロビー・待機・対戦の画面。
- PvP専用の**左右対称マップ**を2種追加（ミラー・アリーナ／クロスファイア）。
  キャンペーンのマップは「左＝自陣／右＝敵陣」の非対称設計で対人戦に使えないため。
  ゲスト（P2）の陣地は `mirrorBase()` で右半分へ鏡写しにする。

### 両者が同じ設定に到達する仕組み

進化段階・バフ・陣地は端末ごとに違うので、**部屋のドキュメントに書き込み、
両者がその同じデータだけから `SimConfig` を組み立てる**。自分の端末のストアは
一切読まない（相手には無い情報なので、読んだ瞬間にデシンクする）。
seed はクライアントが選べないよう、サーバー時刻を素に撹拌して確定させる。

### 通信状態の表示

ロックステップは相手待ちで画面が止まる。無言のフリーズがいちばん悪い体験なので、
`WAITING`（あいてを まっています…）／`DROPPED`（せつだんされました）／
`DESYNC`（ちゅうだんしました・引き分け）を全画面で明示する。
デシンク時に片方だけを勝ちにはしない（両者の画面で結果が食い違っているため）。

### 検証

- `npm run test:sim` … 24項目すべて成功（§39の15項目＋PvPセットアップ9項目）
  - ホスト側とゲスト側が部屋データだけから**完全に同一の SimConfig・初期配置**に到達
  - PvP1試合（915tick）を通して両者の状態が一致し、**同じ勝敗**に到達
  - PvP用マップ2種の地形が左右対称、陣地の鏡写しが正しい
- ヘッドレスChromiumでハブ→たいせん画面の遷移を確認（JSエラーなし）。
  Firebase未設定の環境では「たいせんは まだ つかえません」と理由を表示し、
  ソロプレイには影響しないことを確認。
- PvE（キャンペーン第1章）の回帰確認も実施し、従来どおり勝利まで到達。

### 利用者側で必要な設定

`.env.local` に `VITE_FIREBASE_DATABASE_URL` を追加し、Firebaseコンソールで
Realtime Database を有効化する。未設定でもソロプレイは従来どおり動作する。

## 41. 敵の物量とキャンペーン難易度の再設計（テストプレイ②⑦）

> 「いまテストプレイ（ステージ１と２）しましたが、敵キャラの増加数がやっぱり
> 少なすぎます。にゃんこ大戦争などを参考にしてください」
> 「一番は敵キャラの生成が遅いので、特に低レベル隊を見直してください」

前回も増援レートを上げたが、まだ足りなかった。**なぜ上げても効かなかったのか**を
先に突き止めてから、数値を決め直した。

### 効かなかった原因は2つあった（どちらもレート値ではなかった）

1. **出現間隔に 1500ms の下限があった**（`catwars/sim/simulate.ts`）。
   `Math.max(1500, cost / rate * 1000)` と書かれていたため、レートをいくら
   上げても「1.5秒に1体」で頭打ちになる。実測でも第4〜5章が 1.7秒/体 に
   貼りついており、レートを変えても密度が動かない状態だった。→ 下限を **600ms** へ。

2. **撃破報酬が一律 8⚡ だった**。ネコ1体が 30⚡ なので、**雑兵を4体倒せば
   次の1体が出せる**。つまり「敵を増やす → プレイヤーの⚡収入が増える →
   もっとネコが出せる」という正のフィードバックになっていて、**敵を増やすほど
   プレイヤーが有利**になっていた。二分探索でレートを 89（＝現行の6倍以上）まで
   上げても勝率が **100%から1ポイントも動かなかった**のがその証拠。
   → 報酬を `1 + 最大HP/30` に変更。雑兵は 2⚡、重装ロボは 9⚡、ボスは 31⚡ と
   「大物を倒すと大きい」形にした。

### 計測のやり方を2つ直した

**(a) 章ごとに「そこに着いた子が持っているもの」で測る。**
これまでは全8章を「進化なし・バフなし・拠点なし」で測っていた。しかし
キャンペーンは第1章から順に進むので、第8章に来た子は必ず第7章までを
クリアしている。素の条件で測ると終盤の難易度を実際より厳しく見積もる。
`scripts/botSim.ts` の `LOADOUT_BY_CHAPTER` に、章ごとの想定装備
（進化段階・拠点・バフ）を定義した。`BARE=1` で従来どおりの下限チェックもできる。

**(b) 3段階の上手さで挟む。**
ボットはミスをせず必ず一定間隔でクイズを解くので、実在の小4より上手い。
1本で測ると難易度を甘く見積もる。「じょうず(10秒/問)・ふつう(14秒/問)・
にがて(24秒/問・役割を意識しない)」の3体で測り、**「にがて」が目標勝率に
届いていること**を合格条件にした（設計仕様の目標値は実在の子ども向けの
数字なので、安全側で見積もる）。

### 生産施設の寄与を「加算」から「倍率」へ

`ENEMY_PRODUCTION_RATE` はもとは「アーミーキャンプなら毎秒+3.0」の加算だった。
ところが章が進むほど基本値そのものが大きくなるので、施設の比重が薄まる。
実測では第3〜5章で**施設を全部こわしても増援が2割しか遅くならず**
（0.91秒/体 → 1.13秒/体）、いちばん教えたい「キャンプをつぶすと楽になる」という
因果が体感できなくなっていた。

倍率（キャンプ ×0.35 / 兵舎 ×0.30 / 金山 ×0.12 を加算した係数を掛ける）に
変えたことで、どの章でも**全壊で 4〜6割減**と同じ割合で効くようになった。

| 章 | 施設健在 | 湧き口(キャンプ・兵舎)を全壊 | 生産施設を全壊 |
|---|---|---|---|
| 1 | 1.82秒/体 | 2.33秒/体 | 2.89秒/体 |
| 2 | 1.62秒/体 | 2.56秒/体 | 2.87秒/体 |
| 3 | 0.91秒/体 | 1.44秒/体 | 1.61秒/体 |
| 4 | 0.91秒/体 | 1.43秒/体 | 1.60秒/体 |
| 5 | 0.84秒/体 | 1.55秒/体 | 1.74秒/体 |
| 6 | 1.33秒/体 | 2.46秒/体 | 2.75秒/体 |
| 7 | 1.01秒/体 | 1.90秒/体 | 2.13秒/体 |
| 8 | 1.41秒/体 | 2.89秒/体 | 3.58秒/体 |

### 決めた値と実測（`npm run test:balance`）

レートは `scripts/tuneRates.ts` の二分探索で章ごとに決めた（各点12〜20試合）。

| 章 | 目標 | じょうず | ふつう | にがて | 判定 | 平均決着 | 敵の出現数 | 出現間隔 | 同時最大 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 85% | 100% | 100% | 100% | ✅ | 37秒 | 13.0体 | 2.9秒/体 | 26体 |
| 2 | 85% | 100% | 100% | 100% | ✅ | 117秒 | 64.9体 | 1.8秒/体 | 39体 |
| 3 | 70% | 100% | 100% | 75% | ✅ | 197秒 | 190.8体 | 1.0秒/体 | 67体 |
| 4 | 70% | 100% | 100% | 83% | ✅ | 229秒 | 229.4体 | 1.0秒/体 | 50体 |
| 5 | 70% | 100% | 100% | 80% | ✅ | 162秒 | 176.3体 | 0.9秒/体 | 85体 |
| 6 | 70% | 100% | 100% | 83% | ✅ | 199秒 | 134.8体 | 1.5秒/体 | 58体 |
| 7 | 55% | 100% | 92% | 58% | ✅ | 278秒 | 254.2体 | 1.1秒/体 | 48体 |
| 8 | 55% | 100% | 92% | 58% | ✅ | 279秒 | 187.3体 | 1.5秒/体 | 54体 |

（決着時間・出現数・同時最大は「にがて」の値。同時最大＝盤上に同時に居た
エンティティ数の上限で、描画負荷の目安）

前回の実測（素の条件・ふつう1体）と比べると、**1試合に出てくる敵の数は
第1章で 7.7体 → 13.0体、第3章で 47体 → 191体、第7章で 136体 → 254体**になった。

### 第8章のボス出現タイミング

`bossAtSpawnCount`（何体めの増援でボスが割りこむか）は、増援ペースを上げるたびに
実時間が早まってしまう。今回のペース（施設健在で1.41秒/体）に合わせて
**45 → 70** に引き直した（開始からおよそ103秒）。生産施設をこわして増援を
止めるほどボスも遅れて来るので、攻略のごほうびとしても働く。

### 検証

- `npm run test:balance` … 全8章で「にがて」の勝率が目標±15ポイント以内。
- `npm run test:sim` … 決定論テスト24項目すべて成功（撃破報酬・出現間隔の変更後も
  リプレイ一致・PvP同期は維持）。
- `npx tsc --noEmit` … エラーなし。
- ヘッドレスChromiumで第1章を実プレイ … 57秒で勝利、平均53.8fps、JSエラーなし。
  「敵のアーミーキャンプから増援が出てきた」「アーミーキャンプを破壊！ この場所から
  増援が出てこなくなった」「金山を破壊！ 敵の増援が おそくなった」の3種を確認。
- いちばん敵の多い**第5章**でも実プレイ … 同時71体を描画して**平均53.0fps**、
  最悪フレーム100ms、JSエラーなし。物量を増やしても描画は破綻していない。
